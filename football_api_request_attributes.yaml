country:
  url:
    countries
  parameters:
    type: list
    attributes:
      - name
      - code
      - flag

league:
  url:
    leagues
  parameters:
    type: dict
    attributes:
      league:
        type: list
        keys:
          - id
          - name
          - type
          - logo
      country:
        type: list
        keys:
          - name

season:
  url:
    leagues
  parameters:
    type: dict
    attributes:
      league:
        type: list
        keys:
          - id
      seasons:
        type: array
        keys:
          - year
          - start
          - end
          - current
